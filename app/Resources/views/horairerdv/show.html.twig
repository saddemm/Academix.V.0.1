{% extends 'base.html.twig' %}

{% block body %}
    <h1>Horairerdv</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ horaireRDV.id }}</td>
            </tr>
            <tr>
                <th>Heuredebut</th>
                <td>{{ horaireRDV.heureDebut }}</td>
            </tr>
            <tr>
                <th>Heurefin</th>
                <td>{{ horaireRDV.heureFin }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('horairerdv_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('horairerdv_edit', { 'id': horaireRDV.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
