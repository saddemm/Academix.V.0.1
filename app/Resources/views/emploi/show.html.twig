{% extends 'base.html.twig' %}

{% block body %}
    <h1>Emploi</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ emploi.id }}</td>
            </tr>
            <tr>
                <th>Lien</th>
                <td>{{ emploi.lien }}</td>
            </tr>
            <tr>
                <th>Ajoutele</th>
                <td>{% if emploi.ajouteLe %}{{ emploi.ajouteLe|date('Y-m-d') }}{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('emploi_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('emploi_edit', { 'id': emploi.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
